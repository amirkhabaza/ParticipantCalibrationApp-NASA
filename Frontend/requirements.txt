# Participant Calibration App — Python dependencies
# Tested: macOS, Windows 11 (HP EliteBook)
#
# Quick start (from this directory):
#   ./run.sh              # macOS / Linux
#   .\run.ps1             # Windows PowerShell
#
# Manual setup requires Python 3.10 (PsychoPy wheels: 3.8–3.10):
#   python3.10 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   python calibration_9point.py

psychopy>=2024.1.4,<2027.0.0
